import { smallIslandTravellerAnalytics } from './small-island-traveller-analytics';

describe('smallIslandTravellerAnalytics', () => {
  it('should work', () => {
    expect(smallIslandTravellerAnalytics()).toEqual(
      'small-island-traveller-analytics'
    );
  });
});
