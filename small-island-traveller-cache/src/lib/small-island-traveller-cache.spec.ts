import { smallIslandTravellerCache } from './small-island-traveller-cache';

describe('smallIslandTravellerCache', () => {
  it('should work', () => {
    expect(smallIslandTravellerCache()).toEqual('small-island-traveller-cache');
  });
});
