import { smallIslandTravellerDataStore } from './small-island-traveller-data-store';

describe('smallIslandTravellerDataStore', () => {
  it('should work', () => {
    expect(smallIslandTravellerDataStore()).toEqual(
      'small-island-traveller-data-store'
    );
  });
});
