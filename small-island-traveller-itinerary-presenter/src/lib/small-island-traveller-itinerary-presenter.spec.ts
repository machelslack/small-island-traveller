import { smallIslandTravellerItineraryPresenter } from './small-island-traveller-itinerary-presenter';

describe('smallIslandTravellerItineraryPresenter', () => {
  it('should work', () => {
    expect(smallIslandTravellerItineraryPresenter()).toEqual(
      'small-island-traveller-itinerary-presenter'
    );
  });
});
