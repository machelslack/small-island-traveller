import { smallIslandTravellerLanguageTranslator } from './small-island-traveller-language-translator';

describe('smallIslandTravellerLanguageTranslator', () => {
  it('should work', () => {
    expect(smallIslandTravellerLanguageTranslator()).toEqual(
      'small-island-traveller-language-translator'
    );
  });
});
