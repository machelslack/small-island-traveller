import { smallIslandTravellerPaymentGateway } from './small-island-traveller-payment-gateway';

describe('smallIslandTravellerPaymentGateway', () => {
  it('should work', () => {
    expect(smallIslandTravellerPaymentGateway()).toEqual(
      'small-island-traveller-payment-gateway'
    );
  });
});
