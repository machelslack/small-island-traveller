import { smallIslandTravellerRecommendationEngine } from './small-island-traveller-recommendation-engine';

describe('smallIslandTravellerRecommendationEngine', () => {
  it('should work', () => {
    expect(smallIslandTravellerRecommendationEngine()).toEqual(
      'small-island-traveller-recommendation-engine'
    );
  });
});
